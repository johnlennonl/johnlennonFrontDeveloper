var typed = new Typed('#typed-text', {
  strings: [' Frontend Developer'],
  typeSpeed: 50,
  backSpeed: 25,
  loop: true,
  loopCount: Infinity,
  backDelay: 3000,
  // startDelay: 0  // Opcional: elimina el retraso inicial si lo deseas
});
